name: Build C++ Project

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Echo a string
      run: echo "Hello, GitHub Actions!"
